# Runtime dependencies — everything the application needs to serve requests.
# This is the file the Docker image installs, so anything added here ships to
# every deployment. Test and tooling packages belong in requirements-dev.txt.
Flask>=3.1
Flask-SQLAlchemy>=3.1
Flask-Migrate>=4.0
Flask-Login>=0.6
APScheduler>=3.10
python-dotenv>=1.0
Werkzeug>=3.1
regex>=2024.0
Pillow>=10.0
