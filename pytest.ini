[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    error::DeprecationWarning:app.*
markers =
    precheck: a cheap structural check. Runs before everything else, and a
        failure stops the session rather than letting five minutes of unrelated
        tests run on top of a mistake that is already known.
